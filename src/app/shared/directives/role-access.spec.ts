import { RoleAccess } from './role-access';

describe('RoleAccess', () => {
  it('should create an instance', () => {
    const directive = new RoleAccess();
    expect(directive).toBeTruthy();
  });
});
